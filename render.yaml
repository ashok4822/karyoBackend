services:
  - type: web
    name: karyo-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Add all other env vars manually in the Render dashboard
      # (never commit secrets to this file)
